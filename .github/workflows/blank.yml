name: Test Rust Server 236

on:
  workflow_dispatch:
    inputs:
      duration:
        description: 'Время работы (минуты)'
        default: '90'
        type: choice
        options:
          - 60
          - 90
          - 120
          - 150

jobs:
  run-server:
    runs-on: ubuntu-latest
    timeout-minutes: 155
    
    steps:
      - uses: actions/checkout@v4
      
      - name: Download server from Release
        run: |
          echo "⬇️ Скачиваем сервер из Release v1.0..."
          curl -L \
            --max-time 900 \
            --retry 3 \
            --progress-bar \
            "https://github.com/fakerplayers/govno1337/releases/download/v1.0/linux236dev.1.zip" \
            -o server.zip 2>&1 | tail -20
          
          ls -lh server.zip
          file server.zip
          
          SIZE=$(stat -c%s server.zip)
          if [ $SIZE -lt 1000000000 ]; then
            echo "❌ Файл слишком маленький!"
            exit 1
          fi
          echo "✅ Скачано: $((SIZE / 1024 / 1024)) MB"
      
      - name: Extract server
        run: |
          mkdir -p server
          unzip -q server.zip -d server/
          SERVER_PATH=$(find server/ -name "RustDedicated" -type f -exec dirname {} \; | head -1)
          echo "SERVER_PATH=$SERVER_PATH" >> $GITHUB_ENV
          echo "📁 Путь: $SERVER_PATH"
      
      - name: Setup Ngrok tunnel
        env:
          NGROK_TOKEN: "3AaFVAMGeE8EIfGuE0zcZCKP8Xc_53T7XzATbLepKz3L9KExX"  # <-- Правильный токен
        run: |
          echo "🔧 Устанавливаем ngrok..."
          curl -s https://ngrok-agent.s3.amazonaws.com/ngrok.asc | sudo tee /etc/apt/trusted.gpg.d/ngrok.asc >/dev/null
          echo "deb https://ngrok-agent.s3.amazonaws.com buster main" | sudo tee /etc/apt/sources.list.d/ngrok.list
          sudo apt update && sudo apt install ngrok -y
          
          echo "🌐 Запускаем туннель..."
          ngrok tcp 28015 --authtoken "$NGROK_TOKEN" &
          sleep 20
          
          # Проверяем туннель
          TUNNEL=$(curl -s http://localhost:4040/api/tunnels | grep -o '"public_url":"tcp://[^"]*"' | head -1)
          
          if [ -z "$TUNNEL" ]; then
            echo "❌ Туннель не создан!"
            exit 1
          fi
          
          echo ""
          echo "╔════════════════════════════════════════════════╗"
          echo "║  🎮 SERVER READY FOR CONNECTION                ║"
          echo "╠════════════════════════════════════════════════╣"
          echo "║  $TUNNEL"
          echo "╚════════════════════════════════════════════════╝"
          echo ""
          CONNECT_ADDR=$(echo $TUNNEL | sed 's/.*tcp://' | tr -d '"')
          echo "👉 Введи в консоли Rust: client.connect $CONNECT_ADDR"
      
      - name: Start Rust Server
        run: |
          cd "$SERVER_PATH"
          chmod +x RustDedicated
          
          ./RustDedicated -batchmode -nographics \
            +server.port 28015 \
            +server.queryport 28016 \
            +rcon.port 28017 \
            +rcon.password "test12345" \
            +rcon.web 1 \
            +server.hostname "GitHub Test 236" \
            +server.description "Test server" \
            +server.maxplayers 2 \
            +server.worldsize 1000 \
            +server.seed 12345 \
            +server.level "Barren" &
          
          sleep 240
          
          if pgrep -f RustDedicated; then
            echo "✅ Сервер работает!"
          else
            echo "❌ Ошибка запуска"
            exit 1
          fi
          
          # Показываем адрес ещё раз
          echo ""
          TUNNEL=$(curl -s http://localhost:4040/api/tunnels | grep -o '"public_url":"tcp://[^"]*"' | head -1)
          echo "🎮 $TUNNEL"
          echo "👉 client.connect $(echo $TUNNEL | sed 's/.*tcp://' | tr -d '"')"
          
          DURATION=${{ github.event.inputs.duration || '90' }}
          echo "⏱️ Работаем $DURATION минут..."
          
          for i in $(seq 1 $DURATION); do
            sleep 60
            [ $((i % 15)) -eq 0 ] && echo "⏳ $i / $DURATION мин"
          done
      
      - name: Cleanup
        if: always()
        run: pkill -f RustDedicated; pkill -f ngrok

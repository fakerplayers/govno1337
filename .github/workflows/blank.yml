name: Test Rust Server 236

on:
  workflow_dispatch:
    inputs:
      duration:
        description: 'Время работы (минуты)'
        default: '90'
        type: choice
        options:
          - 60
          - 90
          - 120
          - 150

jobs:
  run-server:
    runs-on: ubuntu-latest
    timeout-minutes: 155
    
    steps:
      - uses: actions/checkout@v4
      
      - name: Download server from Release
        run: |
          echo "⬇️ Скачиваем сервер из Release v1.0..."
          echo "Это может занять 5-10 минут (1.61 GB)..."
          
          # Скачиваем ZIP с правильным именем (linux236dev.1.zip)
          wget -q --show-progress \
            "https://github.com/${{ github.repository }}/releases/download/v1.0/linux236dev.1.zip" \
            -O server.zip
          
          # Проверяем
          echo "✅ Скачано:"
          ls -lh server.zip
          file server.zip
          
          # Проверка размера (должно быть > 1 GB)
          SIZE=$(stat -c%s server.zip)
          if [ $SIZE -lt 1000000000 ]; then
            echo "❌ Файл слишком маленький: $SIZE байт"
            exit 1
          fi
          echo "📦 Размер: $((SIZE / 1024 / 1024)) MB"
      
      - name: Extract ZIP
        run: |
          echo "📦 Распаковываем server.zip..."
          mkdir -p server
          unzip -q server.zip -d server/
          
          echo "✅ Распаковка завершена!"
          
          # Ищем путь к RustDedicated
          if [ -f "server/RustDedicated" ]; then
            SERVER_PATH="server"
          else
            SERVER_PATH=$(find server/ -name "RustDedicated" -type f -exec dirname {} \; | head -1)
          fi
          
          if [ -z "$SERVER_PATH" ]; then
            echo "❌ Не найден RustDedicated!"
            echo "Содержимое server/:"
            ls -la server/
            find server/ -type f | head -20
            exit 1
          fi
          
          echo "SERVER_PATH=$SERVER_PATH" >> $GITHUB_ENV
          echo "📁 Путь к серверу: $SERVER_PATH"
          ls -la "$SERVER_PATH/"
      
      - name: Setup Ngrok tunnel
        env:
          NGROK_AUTHTOKEN: ${{ secrets.NGROK_AUTHTOKEN }}
        run: |
          echo "🔧 Устанавливаем ngrok..."
          curl -s https://ngrok-agent.s3.amazonaws.com/ngrok.asc | sudo tee /etc/apt/trusted.gpg.d/ngrok.asc >/dev/null
          echo "deb https://ngrok-agent.s3.amazonaws.com buster main" | sudo tee /etc/apt/sources.list.d/ngrok.list
          sudo apt update && sudo apt install ngrok -y
          
          echo "🌐 Запускаем туннель..."
          ngrok tcp 28015 --authtoken $NGROK_AUTHTOKEN &
          sleep 15
          
          # Получаем адрес
          TUNNEL=$(curl -s http://localhost:4040/api/tunnels | grep -o '"public_url":"tcp://[^"]*"' | head -1)
          echo ""
          echo "╔════════════════════════════════════════════════╗"
          echo "║  🎮 SERVER READY FOR CONNECTION                ║"
          echo "╠════════════════════════════════════════════════╣"
          echo "║  $TUNNEL"
          echo "╚════════════════════════════════════════════════╝"
      
      - name: Start Rust Server
        run: |
          cd "$SERVER_PATH"
          
          # Права на выполнение
          chmod +x RustDedicated
          chmod +x runds.sh 2>/dev/null || true
          
          echo "🚀 Запускаем сервер..."
          ./RustDedicated -batchmode -nographics \
            +server.port 28015 \
            +server.queryport 28016 \
            +rcon.port 28017 \
            +rcon.password "test12345" \
            +rcon.web 1 \
            +server.hostname "GitHub Test 236" \
            +server.description "Test server for plugins" \
            +server.maxplayers 2 \
            +server.worldsize 1000 \
            +server.seed 12345 \
            +server.level "Barren" \
            +server.saveinterval 600 &
          
          SERVER_PID=$!
          echo "PID: $SERVER_PID"
          
          # Ждём инициализации (4 минуты)
          echo "⏳ Ждём загрузку сервера (4 минуты)..."
          sleep 240
          
          # Проверяем, что сервер жив
          if ps -p $SERVER_PID > /dev/null; then
            echo "✅ Сервер запущен успешно!"
          else
            echo "❌ Сервер упал!"
            tail -n 50 oxide/logs/oxide_*.txt 2>/dev/null || true
            exit 1
          fi
          
          # Показываем адрес ещё раз
          echo ""
          echo "=== 🎮 ИНФОРМАЦИЯ ДЛЯ ПОДКЛЮЧЕНИЯ ==="
          curl -s http://localhost:4040/api/tunnels | grep -o '"public_url":"tcp://[^"]*"' | head -1
          echo ""
          
          # Время работы
          DURATION=${{ github.event.inputs.duration || '90' }}
          echo "⏱️ Сервер будет работать $DURATION минут"
          echo "🎮 Подключайся и тестируй плагин!"
          echo "⚠️ Авто-выключение через $DURATION минут"
          
          # Обратный отсчёт
          for i in $(seq 1 $DURATION); do
            sleep 60
            if [ $((i % 15)) -eq 0 ]; then
              echo "⏳ Прошло $i / $DURATION минут | Осталось: $((DURATION - i)) мин"
            fi
          done
          
          echo "⏰ Время работы истекло"
      
      - name: Stop Server
        if: always()
        run: |
          echo "🛑 Останавливаем сервер..."
          pkill -f RustDedicated 2>/dev/null || true
          pkill -f ngrok 2>/dev/null || true
          echo "✅ Готово!"
